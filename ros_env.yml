name: ros_env
channels:
  - conda-forge
  - robostack
  - defaults
dependencies:
  - python=3.8
  - ros-noetic-desktop
  - ros-noetic-rqt
  - ros-noetic-rqt-image-view
  - opencv
  - pip
  - pip:
      - numpy
      - torch
      - torchvision
      - ultralytics
      - supervision
